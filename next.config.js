/** @type {import('next').NextConfig} */
const nextConfig = {
  distDir: "build",
};

module.exports = nextConfig;

module.exports = {
  
    images: {
      domains: ['imgfz.com', 'shields.io'],
    },
  }